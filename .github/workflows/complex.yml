name: complex
on: push
jobs:
  ubuntu:
    runs-on: ubuntu-latest
    steps:
    - run: date
  windows:
    runs-on: windows-latest
    steps:
    - run: date
  macros:  
    runs-on: macros-latest
    steps:
    - run: date
  depends:
    needs: [ubuntu, windows, macros]
    runs-on: macros-latest
    steps:
    - run: date
  
